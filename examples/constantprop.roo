procedure main ()
  integer i;
  boolean b;
{
  i <- 2 * 5 + (24 / (3 - 1));
  b <- 5 < 8 or 5 > 8/0; # runtime failure
  b <- 1 < 2 and 5 > (3 + 2) or (true = not false);
  writeln i;
  writeln b;
}
